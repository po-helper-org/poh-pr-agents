"""Отказоустойчивое ядро PR-Agent — доменные модули (Фаза 1, issue #1).

Реализует проверяемые системные требования из ../SYSTEM-REQUIREMENTS.md как
модули без внешних зависимостей (stdlib-only). FastAPI-обвязка, брокер и
reconciliation sweeper подключаются следующими срезами.
"""
